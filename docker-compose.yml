services:
  streamx-bot:
    container_name: streamXBot

    build:
      context: .
      dockerfile: Dockerfile

    command: ["bash", "/app/start.sh"]

    ports:
      - "127.0.0.1:8001:8001"

    restart: unless-stopped
